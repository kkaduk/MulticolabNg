include "application.conf"

pekko {
  // Run locally without remoting/cluster to avoid port 2556 conflicts
  actor {
    provider = "local"
  }
  remote.artery {
    canonical.hostname = "127.0.0.1"
    canonical.port = 0
  }
  cluster {
    seed-nodes = []
  }
}
